# GLAMDRING :: dependencias minimas para arrancar.
#
# El nucleo (normalizadores, extraccion, grafo) solo necesita fastapi+pydantic.
# httpx solo hace falta para los conectores en vivo; los SDK de Azure son
# opcionales y se importan dentro de la funcion que los usa, de modo que un
# despliegue que solo trabaje con ficheros no los necesita.

fastapi>=0.110
uvicorn[standard]>=0.27
pydantic>=2.6
python-multipart>=0.0.9      # imprescindible para la subida de ficheros

# --- conectores en vivo -----------------------------------------------------
httpx>=0.27                  # Splunk, QRadar y la via REST de Sentinel

# --- Sentinel por SDK (opcional; sin esto se usa la via REST) ---------------
# azure-monitor-query>=1.3
# azure-identity>=1.16
